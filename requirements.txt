# --- Núcleo de la aplicación ---
streamlit>=1.40
pandas>=2.0

# --- Datos de mercado ---
yfinance>=0.2.40

# --- Indicadores técnicos ---
# Nota: usamos 'ta' (estable) en lugar de 'pandas-ta', que ha dado problemas
# de compatibilidad con versiones recientes de pandas/numpy.
ta>=0.11

# --- Visualización ---
plotly>=5.20
matplotlib>=3.7

# --- Reporte PDF ---
reportlab>=4.0
pillow>=10.0

# --- Capa LLM (chatbot) ---
# Cerebro del chatbot: Groq (modelos open-source gratuitos, con límites de uso).
# La clave va en .env (GROQ_API_KEY), nunca en el repositorio.
groq>=0.11
python-dotenv>=1.0

# --- Pruebas ---
pytest>=8.0
pytest-cov>=5.0